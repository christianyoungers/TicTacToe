#ifndef GLOBALS
#define GLOBALS

const int BLOCKDIM = 200;
const int XDIM = 3;
const int YDIM = 3;

const int LINEWIDTH = 10;

const int WIDTH = (BLOCKDIM * XDIM) + 2*LINEWIDTH - 1;
const int HEIGHT = (BLOCKDIM * YDIM) + 2*LINEWIDTH - 1;

#endif
